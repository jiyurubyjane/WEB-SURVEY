<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h1>Selamat Datang di Dashboard, <%= user.nama %>!</h1>
    <p>Anda login sebagai: <strong><%= user.peran %></strong></p>

    <hr>

    <h3>Menu Anda:</h3>
    <% if (user.peran === 'Admin') { %>
        <a href="/kelola-pengguna"><button>Kelola Pengguna</button></a>
        <a href="/kelola-event"><button>Kelola Event</button></a>
        <a href="/input-survei"><button>Input Data Survei</button></a>
    <% } else if (user.peran === 'Analis') { %>
        <a href="/kelola-event"><button>Kelola Event</button></a>
    <% } else if (user.peran === 'Surveyor') { %>
        <a href="/input-survei"><button>Input Data Survei</button></a>
    <% } %>

    <br><br>
    <a href="/logout">Logout</a>
</body>
</html>